<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cetasas.message.mapper.MImageMapper">
    <insert id="insert">
        INSERT INTO `m_image`(`id`, `filename`, `content`, `suffix`)
        VALUES (#{id}, #{filename}, #{content}, #{suffix});
    </insert>

    <select id="selectByPrimaryKey" resultType="cn.cetasas.message.pojo.MImage">
        SELECT * FROM `m_image` WHERE `id` = #{id};
    </select>
</mapper>